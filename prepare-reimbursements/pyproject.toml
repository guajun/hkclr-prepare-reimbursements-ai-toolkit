[project]
name = "prepare-reimbursements"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "pillow>=12.3.0",
]

[dependency-groups]
dev = [
    "pyyaml>=6.0.3",
]
